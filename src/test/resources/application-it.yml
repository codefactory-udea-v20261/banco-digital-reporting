spring:
  cloud:
    vault:
      enabled: false
    discovery:
      enabled: false
eureka:
  client:
    enabled: false
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///banco_digital_reporting_it?TC_DAEMON=true
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  kafka:
    bootstrap-servers: ${spring.kafka.bootstrap-servers:localhost:9092}
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

app:
  security:
    jwt:
      secret: dGVzdF9zZWNyZXRfcmVwbGFjZV9pbl9wcm9kX3dpdGhfMjU2Yml0cw==

management:
  health:
    vault:
      enabled: false

logging:
  level:
    com.udea.bancodigital: DEBUG